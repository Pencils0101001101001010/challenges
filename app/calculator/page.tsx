import { CalculatorPage } from "../components/calculator";

export const metadata = {
  title: "CALCULATOR INTERFACE",
  description: "Need help counting?",
};

export default function Calculator() {
  return <CalculatorPage />;
}
