import MusicPlayerLayout from "../components/(music-player-layout)/MusicPlayerLayout";

export const metadata = {
  title: "Music Player",
  description: "Only ui",
};

function page() {
  return (
    <div>
      <MusicPlayerLayout />
    </div>
  );
}

export default page;
